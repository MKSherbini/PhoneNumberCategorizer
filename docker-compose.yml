version: "3.9"
services:
  backend:
    image: backend
    container_name: backend
    build:
      context: ./backend
    ports:
      - "9000:8888"
  frontend:
    image: frontend
    container_name: frontend
    build:
      context: ./frontend
    ports:
      - "4200:8888"
    depends_on:
      - backend
